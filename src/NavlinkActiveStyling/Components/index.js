import React from "react";
import ReactDOM from "react-dom/client";
import NavlinkActiveStyling from "../App";
import "./App.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <NavlinkActiveStyling/>
  </React.StrictMode>
);
